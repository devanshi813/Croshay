# Spring Data MongoDB configuration
spring.data.mongodb.host=localhost
spring.data.mongodb.port=27017
spring.data.mongodb.database=my-ecommerce-db
spring.data.mongodb.username=your-mongodb-username
spring.data.mongodb.password=your-mongodb-password

# Server port configuration
server.port=8080

# Logging level
logging.level.root=INFO
logging.level.org.springframework.web=INFO

# Firebase configuration
# Replace YOUR_FIREBASE_API_KEY with your actual Firebase API key
firebase.api.key=YOUR_FIREBASE_API_KEY

# Other application-specific properties
# Add any other custom properties that your application requires
